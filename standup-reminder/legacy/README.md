# Legacy LaunchAgent version

Shell script + `launchd` reminders (no Xcode required). Prefer the Swift menu bar app in the parent folder for pause/snooze, Focus/calendar awareness, and actionable notifications.

```bash
cd legacy
chmod +x install.sh uninstall.sh bin/standup-reminder.sh
./install.sh
```
